/**
 * Constants for Resolver decorator
 */

/**
 * Metadata key for resolver classes
 */
export const RESOLVERS_METADATA = Symbol("__mini_resolvers");

/**
 * Metadata key for resolved data
 */
export const RESOLVED_DATA_METADATA = Symbol("__mini_resolved_data");

/**
 * Metadata key for resolver state
 */
export const RESOLVER_STATE_METADATA = Symbol("__mini_resolver_state");
